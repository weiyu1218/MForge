[project]
name = "cig-compiler-svc"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mf-core", "nl2obj", "pydantic>=2.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cig_compiler_svc"]
