[project]
name = "mf-generators-mmpt-rag"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mf-core", "torch>=2.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mf_generators"]
