[project]
name = "fragfm-generator-svc"
version = "0.1.0"
description = "Fragment-level Deep Fragment Model molecule generator service"
requires-python = ">=3.11"
dependencies = [
    "mf-chem",
    "mf-core",
    "mf-humu",
    "mf-generators-fragfm",
    "grpcio>=1.60",
    "grpcio-tools>=1.60",
    "protobuf>=4.25",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "torch>=2.6",
    "rdkit>=2024.9.5",
    "structlog>=24.1",
]

[project.entry-points."moleculeforge.services"]
fragfm_generator = "fragfm_generator_svc"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fragfm_generator_svc"]
