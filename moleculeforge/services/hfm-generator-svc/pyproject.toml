[project]
name = "hfm-generator-svc"
version = "0.1.0"
description = "Hyperbolic Flow Matching 3D molecule generator service"
requires-python = ">=3.11"
dependencies = [
    "mf-chem",
    "mf-core",
    "mf-humu",
    "mf-generators-hfm-3d",
    "grpcio>=1.60",
    "grpcio-tools>=1.60",
    "protobuf>=4.25",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "torch>=2.6",
    "geoopt>=0.5.1",
    "rdkit>=2024.9.5",
    "structlog>=24.1",
]

[project.entry-points."moleculeforge.services"]
hfm_generator = "hfm_generator_svc"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hfm_generator_svc"]
